# Supabase project (publishable anon key — safe to commit; matches the web .env).
# NOTE: the web .env appends /rest/v1/ to the URL; supabase-js needs the bare
# project URL, so it is stripped here.
EXPO_PUBLIC_SUPABASE_URL=https://vokxxczomywnmqvlenmj.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_Z4bdq6eCviMHx1DxKpsS1Q_qwJAz_As

# Express API server (server/). Leave unset during development: the app derives
# http://<dev-machine-LAN-IP>:3002/api from the Expo dev session automatically.
# Set this once the server is deployed somewhere public.
# EXPO_PUBLIC_API_URL=https://api.example.com/api
